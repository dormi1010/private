#include<bits/stdc++.h>
#define REP(i,n) for(int i=1;i<=n;i++)
#define _GLIBCXX_DEBUG
using namespace std;
using ll = long long;
using Pi = pair<int,int>;
using Pl = pair<ll,ll>;

int main(){
    cin.tie(0);
    ios::sync_with_stdio(false);
    int N;
    string S = "abcdefghij";
    vector<string> ans(10000000);
    cin >> N;
    REP(i,N){
        REP(j,i){
            
        }
    }
    }